export default function Heading2({ children }: { children: React.ReactNode }) {
  return (
    <h2 className="text-lg text-[#FFF56D] mb-1 font-bold uppercase tracking-wider">
      {children}
    </h2>
  );
}
